import type { Exercise } from "@/lib/types";

export const TS_ARRAYS_SOME_EVERY: Exercise[] = [
  { id: 1, title: "some() Básico: ¿Al Menos Uno?", stars: 1, category: "SOME", description: "some() retorna true si AL MENOS UN elemento cumple la condición. Se detiene en el primer match.", objective: "Existencia de al menos uno", tags: ["some", "exists", "any"], fileName: "someBasic.ts", completed: false, explanationText: "some() es like SQL EXISTS o LINQ .Any(). Hace short-circuit: se detiene al primer true.", codeSnippet: "const nums = [1, 3, 5, 8, 9];\nconst hasEven = nums.[INPUT_1](n => n % 2 === [INPUT_2]);\n// [INPUT_3]", inputs: { INPUT_1: "some", INPUT_2: "0", INPUT_3: "true" }, completeCode: "nums.some(n => n % 2 === 0) → true (8 es par)" },
  { id: 2, title: "every() Básico: ¿Todos Cumplen?", stars: 1, category: "EVERY", description: "every() retorna true solo si TODOS los elementos cumplen la condición. Se detiene en el primer false.", objective: "Todos cumplen condición", tags: ["every", "all", "validate"], fileName: "everyBasic.ts", completed: false, explanationText: "every() es like SQL ALL o LINQ .All(). Se detiene al primer false.", codeSnippet: "const ages = [18, 21, 25, 30];\nconst allAdults = ages.[INPUT_1](age => age >= [INPUT_2]);\n// [INPUT_3]", inputs: { INPUT_1: "every", INPUT_2: "18", INPUT_3: "true" }, completeCode: "ages.every(age => age >= 18) → true" },
  { id: 3, title: "some() para Validar Permisos", stars: 2, category: "SOME", description: "Verifica si un usuario tiene al menos uno de los permisos requeridos.", objective: "Verificación de permisos", tags: ["some", "permissions", "auth"], fileName: "somePermissions.ts", completed: false, explanationText: "Patrón común en autorización: el usuario necesita AL MENOS UN permiso del set requerido.", codeSnippet: "const userPermissions = ['read', 'write', 'comment'];\nconst requiredAny = ['admin', 'write'];\n\nconst hasAccess = requiredAny.[INPUT_1](\n  perm => userPermissions.[INPUT_2](perm)\n);\n// [INPUT_3] — tiene 'write'", inputs: { INPUT_1: "some", INPUT_2: "includes", INPUT_3: "true" }, completeCode: "requiredAny.some(perm => userPermissions.includes(perm))" },
  { id: 4, title: "every() para Validar Formulario", stars: 2, category: "EVERY", description: "Verifica que TODOS los campos del formulario estén completos antes de enviar.", objective: "Validación de formulario", tags: ["every", "form", "validate"], fileName: "everyForm.ts", completed: false, explanationText: "every() es perfecto para validación: el formulario solo es válido si TODOS los campos lo son.", codeSnippet: "interface Field { name: string; value: string; required: boolean; }\nconst fields: Field[] = [\n  { name: 'email', value: 'iram@test.com', required: true },\n  { name: 'name', value: '', required: true }\n];\n\nconst isValid = fields.[INPUT_1](\n  f => !f.[INPUT_2] || f.value.[INPUT_3]() !== ''\n);\n// [INPUT_4] — 'name' está vacío y es requerido", inputs: { INPUT_1: "every", INPUT_2: "required", INPUT_3: "trim", INPUT_4: "false" }, completeCode: "fields.every(f => !f.required || f.value.trim() !== '')" },
  { id: 5, title: "some() para Detectar Duplicados", stars: 3, category: "SOME", description: "Detecta si hay elementos duplicados comparando cada elemento con su primera aparición.", objective: "Detección de duplicados", tags: ["some", "duplicate", "unique"], fileName: "someDuplicate.ts", completed: false, explanationText: "Si el índice del find es diferente al índice actual, hay un duplicado anterior.", codeSnippet: "const emails = ['a@t.com', 'b@t.com', 'a@t.com', 'c@t.com'];\n\nconst hasDuplicates = emails.[INPUT_1](\n  (email, idx, arr) => arr.[INPUT_2](email) !== [INPUT_3]\n);\n// [INPUT_4] — 'a@t.com' aparece dos veces", inputs: { INPUT_1: "some", INPUT_2: "indexOf", INPUT_3: "idx", INPUT_4: "true" }, completeCode: "emails.some((email, idx, arr) => arr.indexOf(email) !== idx)" },
  { id: 6, title: "every() para Type Guard de Array", stars: 3, category: "EVERY", description: "Usa every() con type predicate para verificar que TODOS los elementos de un array son de cierto tipo.", objective: "Type guard para arrays completos", tags: ["every", "is", "guard"], fileName: "everyGuard.ts", completed: false, explanationText: "Si every() con type guard retorna true, puedes asertar que el array completo es del tipo verificado.", codeSnippet: "function isStringArray(arr: unknown[]): arr [INPUT_1] string[] {\n  return arr.[INPUT_2](\n    (item): item is [INPUT_3] => typeof item === 'string'\n  );\n}\n\nconst mixed: unknown[] = ['hello', 'world'];\nif ([INPUT_4](mixed)) {\n  // mixed es string[] aquí\n  mixed[0].toUpperCase(); // OK\n}", inputs: { INPUT_1: "is", INPUT_2: "every", INPUT_3: "string", INPUT_4: "isStringArray" }, completeCode: "function isStringArray(arr: unknown[]): arr is string[] { return arr.every(...) }" },
  { id: 7, title: "some() + every(): Lógica Combinada", stars: 3, category: "COMBINADO", description: "Combina some() y every() para validaciones de negocio complejas.", objective: "Combinación de validaciones", tags: ["some", "every", "business"], fileName: "combined.ts", completed: false, explanationText: "Tip: some() = OR lógico sobre el array. every() = AND lógico. Combínalos para reglas complejas.", codeSnippet: "interface CartItem { price: number; qty: number; category: string; }\nconst cart: CartItem[] = [/* ... */];\n\n// ¿Al menos un producto premium?\nconst hasPremium = cart.[INPUT_1](i => i.price > 1000);\n\n// ¿Todos tienen stock (qty > 0)?\nconst allInStock = cart.[INPUT_2](i => i.[INPUT_3] > 0);\n\n// Puede proceder si tiene premium Y todo tiene stock\nconst canCheckout = hasPremium [INPUT_4] allInStock;", inputs: { INPUT_1: "some", INPUT_2: "every", INPUT_3: "qty", INPUT_4: "&&" }, completeCode: "some() para OR, every() para AND. Combinar con && o ||" },
  { id: 8, title: "every() con Arrays Vacíos (Vacuous Truth)", stars: 3, category: "EVERY", description: "every() retorna true para arrays vacíos (vacuous truth). Esto puede causar bugs sutiles.", objective: "Entender vacuous truth", tags: ["every", "empty", "vacuous"], fileName: "everyEmpty.ts", completed: false, explanationText: "CUIDADO: [].every(() => false) retorna true! Es la 'verdad vacía' de la lógica formal.", codeSnippet: "const empty: number[] = [];\n\n// ¡CUIDADO! Retorna true aunque no hay elementos\nconst result = empty.[INPUT_1](() => false);\n// [INPUT_2]\n\n// Protección contra vacuous truth\nfunction allMatch<T>(arr: T[], pred: (item: T) => boolean): boolean {\n  return arr.[INPUT_3] > 0 && arr.every([INPUT_4]);\n}", inputs: { INPUT_1: "every", INPUT_2: "true", INPUT_3: "length", INPUT_4: "pred" }, completeCode: "[].every(() => false) → true! Verifica arr.length > 0 primero." },
  { id: 9, title: "some() para Búsqueda en Nested Arrays", stars: 3, category: "SOME", description: "Usa some() anidado para buscar en estructuras de datos con sub-arrays.", objective: "Búsqueda profunda con some", tags: ["some", "nested", "deep"], fileName: "someNested.ts", completed: false, explanationText: "some() anidado es O(n*m). Para búsquedas frecuentes, construye un Set o Map de lookup.", codeSnippet: "interface Team { name: string; members: string[] }\nconst teams: Team[] = [\n  { name: 'Frontend', members: ['Iram', 'Ana'] },\n  { name: 'Backend', members: ['Luis', 'Pedro'] }\n];\n\n// ¿Iram está en algún equipo?\nconst iramExists = teams.[INPUT_1](\n  team => team.[INPUT_2].[INPUT_3]('[INPUT_4]')\n);", inputs: { INPUT_1: "some", INPUT_2: "members", INPUT_3: "includes", INPUT_4: "Iram" }, completeCode: "teams.some(team => team.members.includes('Iram'))" },
  { id: 10, title: "Función Genérica: satisfiesAll / satisfiesAny", stars: 4, category: "COMBINADO", description: "Crea utilidades genéricas que acepten múltiples predicados y verifiquen con every/some.", objective: "Multi-predicado genérico", tags: ["generic", "predicates", "compose"], fileName: "multiPredicate.ts", completed: false, explanationText: "Tip senior: Componer predicados es más limpio que condicionales complejos y es altamente testable.", codeSnippet: "type Predicate<T> = (item: T) => boolean;\n\nfunction satisfiesAll<T>(\n  item: T,\n  ...predicates: Predicate<[INPUT_1]>[]\n): boolean {\n  return predicates.[INPUT_2](pred => pred(item));\n}\n\nfunction satisfiesAny<T>(\n  item: T,\n  ...predicates: Predicate<T>[]\n): boolean {\n  return predicates.[INPUT_3](pred => [INPUT_4](item));\n}", inputs: { INPUT_1: "T", INPUT_2: "every", INPUT_3: "some", INPUT_4: "pred" }, completeCode: "satisfiesAll → predicates.every()\nsatisfiesAny → predicates.some()" }
];
